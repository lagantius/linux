/* Global settings */
* {
    border: none;
    border-radius: 10px;
    font-family: JetBrainsMono Nerd Font, monospace;
    font-size: 13px;
    min-height: 10px;
    padding: 0 3px;
    padding-bottom: 0;
    padding-top: 2px;
    color: white;
}

/* Waybar window */
window#waybar {
    background-color: #000000; /* Pure black */
    color: white;
    border: none;
}

/* Default module styling */
#clock,
#network,
#pulseaudio,
#tray,
#battery,
#backlight,
#custom-* {
    background-color: transparent;
    border: 1px solid white;
    margin: 2px 4px;
    padding: 4px 10px;
}

/* Active workspace (if using Sway or Hyprland) */
#workspaces button {
    color: white;
    background-color: #111111;
    border: 1px solid white;
    padding: 2px 6px;
    margin: 1px 2px;
}

/* Focused workspace */
#workspaces button.focused {
    background-color: black;
    color: #FFD700;
    border: 1px solid #FFD700;
}

/* Hovered workspace (optional) */
#workspaces button:hover {
    background-color: #333333;
}

/* Separator (optional) */
#custom-separator {
    color: white;
}

